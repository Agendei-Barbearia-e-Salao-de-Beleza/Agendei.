package agendei.app.api

/* 
// Supabase desativado temporariamente para focar no Design
import io.github.jan.supabase.createSupabaseClient
import io.github.jan.supabase.auth.Auth
import io.github.jan.supabase.postgrest.Postgrest
import io.github.jan.supabase.realtime.Realtime

object SupabaseClient {
    private const val SUPABASE_URL = "https://vpalasmdcxnhpsbwmsqq.supabase.co"
    private const val SUPABASE_KEY = "sb_publishable_4Gy_GS1rdX_oGlzxYgF8Sg_snXLmTdO"

    val client = createSupabaseClient(
        supabaseUrl = SUPABASE_URL,
        supabaseKey = SUPABASE_KEY
    ) {
        install(Postgrest)
        install(Auth)
        install(Realtime)
    }
}
*/
